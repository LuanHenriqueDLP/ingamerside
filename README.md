A simple online store for geek and nerd products built without external payment APIs. Orders are submitted directly to the store instead of being processed through an online payment gateway.
Before running the project, execute the SQL scripts (.txt file) located in the "projeto_igs" folder to initialize the database and test the application's features.
